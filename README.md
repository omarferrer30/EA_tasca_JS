# tasca_JS: 
Use three java script high level functions combined together. In my case, the code fetches an array of users, it filters them by odd IDs, sort them from greater to minor Id and maps the usernames.
